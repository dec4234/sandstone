# mc-data
An experimental crate for auto-generating structs for blocks and items.

Data is pulled from https://github.com/PrismarineJS/minecraft-data

** Was significantly AI generated since its all based on JSON files.